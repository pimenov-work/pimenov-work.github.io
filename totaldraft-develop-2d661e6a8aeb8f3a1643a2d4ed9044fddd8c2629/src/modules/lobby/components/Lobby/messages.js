import { defineMessages } from 'react-intl';

const messages = defineMessages({
  pageTitle: {
    id: 'lobby.pageTitle',
    defaultMessage: 'Лобби',
    description: 'Лобби',
  },
});

export default messages;
